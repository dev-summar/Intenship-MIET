# API base URL for the backend. Required for production.
# Examples:
#   Development (same host): /api
#   Production (same host): /api
#   Production (different host): https://api.yourdomain.com
VITE_API_BASE_URL=/api
